spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SystemAdministrationModule?serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1253846077a
  mvc:
    static-path-pattern: /static/**
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always

server:
  port: 8082
  servlet:
    session:
      timeout: 1800
      cookie:
        same-site: lax
        secure: false


mybatis:
  mapper-locations: classpath:mapper/*xml

logging:
  level:
    com:
      example:
        salarymanage:
          mapper: debug

